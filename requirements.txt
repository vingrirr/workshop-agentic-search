# Standalone project dependencies (no langchain, no elasticsearch).
# Everything else it uses is in the Python standard library (subprocess, csv, json).
requests>=2.31
python-dotenv>=1.0
psycopg[binary]>=3.1

# To run the ORIGINAL Jupyter notebooks instead, use:
#   pip install -r requirements-notebooks.txt
